- name: Deploy Machine Learning Services
  hosts:
    - machine_learning
  remote_user: root
  gather_facts: true

  tasks:
    - name: Setup NVidia Container Toolkit
      block:
        - import_role:
            name: docker

        - import_role:
            name: nvidia-docker